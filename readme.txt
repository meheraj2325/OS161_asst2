
File location : /home/meheraj/Documents/OS/asst02/src/kern/thread/thread.c

- By default, This program will run in FIFO. As, the following line added to the file:

	#define FIFO 
  
- To run in PRIORITY scheduling mode, the following line needs to be added to the specified file:  #define PRIORITY_SCHEDULING
	and following line needs to commented out : #define FIFO
 
  
